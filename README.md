# ApplicationForDownloadFiles
A simple web application that allows you to download file. 

The application implements the possibility of registration and authorization. 
Any user has the ability to download TXT files, but other extensions can only be downloaded by registered users with access. 
Access parameters can only be changed by the "admin" user.

The application uses Hibernate, PostgreSQL, Servlet API.
